/**
 * Copyright (c) 2026 mol* contributors, licensed under MIT, See LICENSE file for more info.
 *
 * @author Alexander Rose <alexander.rose@weirdbyte.de>
 */
import { Loci } from '../mol-model/loci.js';
import { Structure, StructureElement } from '../mol-model/structure.js';
import { Script } from '../mol-script/script.js';
export { Wiggle };
type Wiggle<T extends Loci = Loci> = {
    readonly kind: T['kind'];
    readonly layers: ReadonlyArray<Wiggle.Layer<T>>;
};
declare function Wiggle<T extends Loci>(kind: T['kind'], layers: ReadonlyArray<Wiggle.Layer<T>>): Wiggle;
declare namespace Wiggle {
    type Layer<T extends Loci = Loci> = {
        readonly loci: T;
        readonly value: number;
    };
    const Empty: Wiggle;
    function areEqual(wA: Wiggle, wB: Wiggle): boolean;
    function isEmpty(wiggle: Wiggle): boolean;
    function remap(wiggle: Wiggle, structure: Structure): Wiggle;
    function merge(wiggle: Wiggle): Wiggle;
    function filter(wiggle: Wiggle, filter: Structure): Wiggle;
    type ScriptLayer = {
        script: Script;
        value: number;
    };
    function ofScript(scriptLayers: ScriptLayer[], structure: Structure): Wiggle;
    type BundleLayer = {
        bundle: StructureElement.Bundle;
        value: number;
    };
    function ofBundle(bundleLayers: BundleLayer[], structure: Structure): Wiggle;
    function toBundle(wiggle: Wiggle<StructureElement.Loci>): {
        kind: string;
        layers: BundleLayer[];
    };
}
